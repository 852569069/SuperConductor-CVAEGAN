# Tc prediction model based on one-dimensional vectors

# Useage

Insert the chemical formula to be predicted into the "formula" parameter in the "predict.py" file. The model will automatically load the weight file and network, and print out the prediction results.
